# PROJECT.md

> Generated by `/gsd-ingest-docs` on 2026-07-01.
> Source intel: `.planning/intel/{constraints,context,SYNTHESIS}.md`
> Conflict status: 0 BLOCKERS, 0 WARNINGS, 1 INFO (auto-resolved) — see `.planning/INGEST-CONFLICTS.md`.

## Identity

- **Project**: CS2-Box
- **Mod ID**: `csgobox`
- **Current version**: `1.0.5` (with version-suffixed builds for v26_1_2: `1.0.5-26.1.2`)
- **Author / Owner**: wikkd
- **Repo root**: `/Users/shuangyuexingxun/Desktop/CS2-Box`

## Scope (in-scope)

- CS2-style box opening animation in Minecraft: 2-row × 10-column item preview, scrolling reel, grade-tagged loot, server-authoritative RNG, `/csbox` admin command, mob drop integration.
- Four-tier key progression: iron / gold / diamond / netherite (the last via smithing only).
- Achievement system (A Fresh Start, Shopper) wired through Minecraft native `CriteriaTriggers`.
- Multi-version support: `v1_21_1` (MC 1.21.1 / NeoForge 21.1.115) AND `v26_1_2` (MC 26.1.2 / NeoForge 26.1.2.76) via shared `common` module.

## Out-of-scope (non-goals)

- Cloth Config dependency — explicitly removed; NeoForge native `ModConfigSpec` only.
- Custom 1.20.1 / Forge support — backport is not planned.
- Server-side economy integration — no player-to-player trading; loot is bind-on-open by design.

## Constraints (locked-feeling architectural boundaries)

Source: `.planning/intel/constraints.md` (6 entries).

| ID | Constraint | Source |
|---|---|---|
| CONSTRAINT-001 | `common` MUST NOT import `net.minecraft.*` or `net.neoforged.*` at compile time | SPEC: multiloader-execution-spec.md §1.2 |
| CONSTRAINT-002 | Resources layered: `common/src/main/resources/` (shared) > platform modules > `runs/` runtime overlays. Recipe path is singular `data/csgobox/recipe/` | SPEC + AGENTS.md |
| CONSTRAINT-003 | Build artifacts named `csbox-1.21.1-1.0.5.jar` and `csbox-26.1.2-1.0.5.jar` | SPEC + gradle.properties |
| CONSTRAINT-004 | Java toolchain diverges per platform: `v1_21_1` Java 21, `v26_1_2` Java 25 + `--enable-preview` | SPEC + port-26.1.2.md |
| CONSTRAINT-005 | Refactor phases have explicit acceptance gates; rollback if criteria unmet | SPEC §2 |
| CONSTRAINT-006 | `CONFIG` is `final` (no null guards); preserve `CsgoBox.java` config init order; preserve v1.0.5 functionality (animation, drop, command, achievements, four keys, network sync) | SPEC + AGENTS.md |

## Locked decisions

None ingested as formal ADR. Quasi-ADR invariants from `AGENTS.md` and `multiloader-refactor-plan.md` are recorded in `intel/context.md` but NOT yet locked — see "Open items" below.

## Open items (need user decision)

- **Convert `AGENTS.md` invariants to ADR?** Currently encoded as DOC. If user wants these locked, promote to ADR with `Status: Accepted` and re-run ingest.
- **Define PRD acceptance criteria for refactor phases?** Currently no PRD; phase acceptance lives inside the SPEC. If user wants phase gates tracked as formal requirements, author a PRD and re-run.

## Codebase intel

Pre-existing codebase intelligence in `.planning/codebase/`:
- `STACK.md` — Java 21/25, NeoForge 21.1.115 / 26.1.2.76, Gradle 8.11/8.14, NeoGradle 7.0.171/7.1.38
- `ARCHITECTURE.md` — module map and data flow
- `STRUCTURE.md` — package layout
- `CONVENTIONS.md` — Java 21, 4-space indent, 120 char, `Record` for immutable DTOs, NeoForge Codec/StreamCodec
- `INTEGRATIONS.md` — NeoForge APIs used
- `TESTING.md` — GameTest framework
- `CONCERNS.md` — known risks

Forensic report: `.planning/forensics/report-20260629-114516.md` (GSD debug history).

## Pointer map

- `intel/SYNTHESIS.md` — entry point for downstream `/gsd-plan-phase N`
- `intel/constraints.md` — architectural boundaries
- `intel/context.md` — 18 DOC entries in 5 topics
- `INGEST-CONFLICTS.md` — conflict report
- `intel/classifications/*.json` — per-doc classification (audit trail)
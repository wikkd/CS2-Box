# REQUIREMENTS.md

> Generated by `/gsd-ingest-docs` on 2026-07-01.

## Status: NO PRDs INGESTED

This ingest run found **0 PRD documents** (no `docs/prd/`, no `PRD-*.md` filenames, no `## User Stories` or "As a user" content in any classified DOC). Formal requirements with acceptance criteria must be authored separately.

The 18 DOC entries in `intel/context.md` capture implicit requirements at DOC level (runbooks, release notes, guides), but they are NOT tracked with `REQ-{slug}` IDs or acceptance criteria.

## How to populate this file

Two options:

1. **Author a PRD document** with explicit requirements + acceptance criteria, place it under `docs/prd/` or root-level `PRD-*.md`, and re-run `/gsd-ingest-docs`. The synthesizer will then derive `REQ-*` IDs and append them here.
2. **Manually add requirements** by editing this file using the schema below.

## Requirement schema (for future ingestion)

```markdown
### REQ-{slug}

- **ID**: REQ-{slug}
- **Source**: {path-to-PRD}
- **Status**: open | in-progress | done | dropped
- **Description**: {single sentence}
- **Acceptance criteria**:
  - [ ] {criterion 1}
  - [ ] {criterion 2}
- **Scope**: {affected components}
```

## Derived implicit requirements (DOC level, not formally locked)

These are observations from ingested DOCs that imply acceptance gates but are not formally tracked:

| Implied requirement | Source DOC | Status |
|---|---|---|
| v26_1_2 GUI must use `extractRenderState(GuiGraphicsExtractor,...)` not legacy `render(...)` | AGENTS.md (mod API migration) + csbox-gui-26.1.2-fix-guide.md | done (per 2026-07-01 audit) |
| Button text must remain visible (renderBg before renderLabels in `CsLookItemScreen.extractRenderState`) | csbox-gui-26.1.2-fix-guide.md P0-2 | done |
| Main preview in result screen must be centered using shared `previewTextureSize/X/Y` methods | csbox-gui-26.1.2-fix-guide.md P0-1 | done |
| Unconfigured box banner must not be covered by 3D preview (`if (!boxEmpty)` guard) | csbox-gui-26.1.2-fix-guide.md P0-3 | done |
| Tick-sound playback throttled to 120ms minimum interval | csbox-gui-26.1.2-fix-guide.md §修复 3 + multiloader-execution-spec.md | done |
| `pack.mcmeta` uses `supported_formats` only (no `min_format`/`max_format`) for v26_1_2 | multiloader-execution-spec.md §2 + run log fixes | done |
| 4 csgo_key recipes use bare-string ingredient (no `{"item": ...}` wrapper) | run log fixes + v26.1.2 ingredient dispatch | done |
| Button colors unified via `ButtonPalette` (forest-green OPEN, brick-red DANGER) | csbox-gui-26.1.2-fix-guide.md §4.5 | done |
| Text width-limited via `RenderFontTool.drawStringClamped` (maxPixelWidth + ellipsis) | csbox-gui-26.1.2-fix-guide.md §4.6 | done |
| Resource path invariant: `data/csgobox/recipe/` (singular) | AGENTS.md line 59 + multiloader-execution-spec.md CONSTRAINT-002 | done |
| `CONFIG` is final — no null guards in calling code | AGENTS.md + v1.0.5-REVIEW CR-001/CR-002/CR-003 | done |
| `common` module cannot import `net.minecraft.*` or `net.neoforged.*` | multiloader-execution-spec.md + multiloader-refactor-plan.md | done |
| `common/utils/{ColorTools,OverlayColor}` shared across all platform modules | this session (stage 1.1, commit `a0af2ee`) | done |
| `v26_2/` module exists as a third platform alongside v1_21_1 + v26_1_2 | this session (stage 3, commit `ef9e616`) | done (scaffold only) |

## Explicit REQ entries (this session)

### REQ-v26-2-platform

- **ID**: REQ-v26-2-platform
- **Source**: this session (2026-07-01); supersedes deferred full-MultiLoader row above
- **Status**: done
- **Description**: add Minecraft 26.2 / NeoForge 26.2.x as a third supported platform alongside v1_21_1 and v26_1_2.
- **Acceptance criteria**:
  - [x] `v26_2/` Gradle module exists with `build.gradle` + `src/main/{java,resources}` skeleton (commit `ef9e616`)
  - [x] 34 Java files copied from `v26_1_2/` with `v26_1_2 → v26_2` package rename and `Platform26 → Platform26V2` independent impl
  - [x] `settings.gradle` `versionModules` map contains `'26.2' → 'v26_2'`
  - [x] `gradle.properties` has 9 new 26.2 variables block — real values now pinned (commit `e27612f`):
    - `neo_version_26_2 = 26.2.0.7-beta` (latest published; 26.2 still beta)
    - `neogradle_version_26_2 = 7.1.38` (latest userdev)
    - `loader_version_range_26_2 = [11,)` (FML 11.0.13)
    - `pack_format_26_2 = 81` (best estimate, +1 from 26.1.x's 80)
  - [x] Gradle plugin resolution succeeds for v26_2 (verified via `./gradlew :v26_2:compileJava` — NeoGradle 7.1.38 + NeoForge 26.2.0.7-beta + NeoForm 26.2-1 all resolve from `maven.neoforged.net/releases/`)
  - [x] neoForm pipeline runs end-to-end for v26_2 (cacheVersionManifest/decompile/patch/recompile all green)
  - [x] `./gradlew :v26_2:compileJava` succeeds (0 errors — commit `4c9a004` adapted all 9 affected files for the 26.2 decoupled API: PictureInPictureRenderer constructor + renderToTexture signature, Minecraft.setScreen→setScreenAndShow, advancement package restructure, GameRenderer.getLighting→lighting(), Options.hideGui removal)
  - [x] `./gradlew :v26_2:processResources` produces `v26_2/build/libs/csgobox-26.2-1.0.5.jar` (428 KB, pack.mcmeta has `pack_format=81`)
  - [x] Three-module build matrix verified: `:v1_21_1:compileJava` + `:v26_1_2:compileJava` + `:v26_2:compileJava` all BUILD SUCCESSFUL with the common/utils migration + v26_2 scaffold in place
  - [x] Runtime regression verified on v26_2 client (2026-07-01): PIP 3D drag-to-rotate works in `CsboxScreen` + `CsLookItemScreen` after the `PictureInPictureRenderer` rewrite; opening/progress/reveal animations run end-to-end; advancement triggers fire on first open / accumulate toward 200; `/csbox reload` rescans JSON without exceptions
  - [x] HUD-overlay regression acknowledged and accepted by user (2026-07-01): `Options.hideGui` removed in 26.2; hotbar/health bar visible during box-opening screens. No equivalent API in 26.2.0.7-beta. Revisit only if/when 26.2 ships an equivalent.
- **Scope**: `v26_2/` module, `gradle.properties`, `settings.gradle`, possibly `common/` interface additions if B-class abstraction is needed for cross-platform business code reuse (currently deferred per stage 1.1B)

## Deferred requirements (per fix guide §4.4 / §4.7)

| Implied requirement | Source DOC | Status |
|---|---|---|
| Container-based layout abstraction (replace percentage hardcoding) | csbox-gui-26.1.2-fix-guide.md P1-1 | deferred |
| Per-item visual baseline alignment in 2D grid | csbox-gui-26.1.2-fix-guide.md P1-3 | deferred (3D PIP auto-centered; 2D still uses fixed scale) |
| Surface / panel / divider three-tier design tokens | csbox-gui-26.1.2-fix-guide.md P2-2 | deferred |
| Button disabled / pressed states | csbox-gui-26.1.2-fix-guide.md P2-1 follow-up | deferred (only hover currently) |
| Full MultiLoader split into `common` / `v1_21_1` / `v26_1_2` modules (Phase 0-9 of refactor plan) | multiloader-refactor-plan.md + multiloader-execution-spec.md | partial — v26_1_2 + v26_2 exist; common only carries A-class utilities (ColorTools/OverlayColor) + A-platform interface stubs |
| v26_2 runtime regression: PIP 3D rotation, animations, advancement triggers, /csbox reload | stage 6 verification | done |
| v26_2 HUD-overlay regression: hideGui removal accepted by user | stage 6 user ack | done (no action; revisit on 26.2 API addition) |
# 取证报告 — v1.0.5 发布后热补丁集群

**生成时间：** 2026-06-29T03:45:16Z
**范围：** v1.0.5（提交 862ab1f）发布后约 24 小时内跟进的 7 个修正提交
**触发请求：** "修复1.0.5的问题后正式提交1.0.5" — 诊断热补丁集群并为干净的重新发布做准备

---

## 证据摘要

### Git 活动（最近 30 天，2026-05-28 之后）

| 提交 | 日期 (+0800) | 消息 | 文件 | 类型 |
|---|---|---|---|---|
| 5f3856a | 2026-06-29 11:39 | docs(artifacts): add v1.0.5 review + port journal | 2 个文件 / 739+ | docs |
| da28607 | 2026-06-29 11:39 | docs: correct recipe path recipes/ → recipe/ | 3 个文件 / 5+/5- | docs |
| e534161 | 2026-06-29 09:14 | docs(codebase): mechanical csbox→csgobox rename | 6 个文件 / 148+/148- | docs |
| 24a82ac | 2026-06-29 09:00 | docs(codebase): incremental map update for v1.0.5 | 7+4 个文件 / 1515+ | docs+src |
| a8bea6a | 2026-06-29 07:16 | build: force runClient runConfig to JDK 21 | 2 个文件 / 14+ | build |
| b7b11e5 | 2026-06-28 22:02 | fix(config): relocate CsboxConfig to csgobox package | 2 个文件 / 95+ | **fix** |
| be40e5a | 2026-06-28 21:44 | feat(recipe)!: remove workbench 3x netherite recipe | 5 个文件 / 11+/34- | **breaking** |
| 617cbe7 | 2026-06-28 14:22 | chore: add AGENTS.md | 1 个文件 | docs |
| **862ab1f** | **2026-06-28 11:05** | **release: v1.0.5** | 18+ 个文件 | **RELEASE** |
| a86c8c4 | 2026-06-19 16:32 | release: v1.0.4 | 60+ 个文件 | release |

**关键计数：**
- 最后提交：`5f3856a` 于 2026-06-29 11:39:53 +0800（约 6 分钟前）
- v1.0.5 发布：`862ab1f` 于 2026-06-28 11:05:17 +0800
- 间隔：发布到最后一个热补丁约 **24 小时 35 分钟**
- 发布后热补丁：**7 个提交**（617cbe7, be40e5a, b7b11e5, a8bea6a, 24a82ac, e534161, da28607, 5f3856a — 如果把两个新提交都算上实际上是 8 个）
- 其中 **3 个是非文档修复**（be40e5a breaking 变更, b7b11e5 fix, a8bea6a build 修复）

### 仓库标签/分支状态

- 标签：仅存在 `v1.0.0` — `a86c8c4`（v1.0.4）和 `862ab1f`（v1.0.5）均未正式打标签。
- 分支：`main`，与 `origin/main` 同步，工作区干净。
- 活跃工作树：1 个（仅 main — 无孤立 agent 工作树）。

### 关键源码级证据

```text
$ git ls-tree -r 862ab1f | grep -E "config|CsboxConfig"
(无输出)
```

`862ab1f` **不包含** `CsboxConfig.java`。已发布的 v1.0.5 在每个消费点都有悬空的
`import com.reclizer.csgobox.config.CsboxConfig;` 语句（由发布后的 `b7b11e5` 修复）。
证实了代码审查中的 WR-001 — 该发布标签在独立构建时无法编译通过。

### 规划状态

- `.planning/STATE.md`：**不存在**
- `.planning/ROADMAP.md`：**不存在**
- `.planning/config.json`：**不存在**
- `.planning/phases/`：**不存在**
- `.planning/reports/SESSION_REPORT.md`：**不存在**
- `.planning/codebase/`：7 份文档（ARCHITECTURE / CONCERNS / CONVENTIONS / INTEGRATIONS / STACK / STRUCTURE / TESTING）— 存在
- `.planning/v1.0.5-REVIEW.md`：存在（提交 `5f3856a`，中文编写）

这是一个**非 GSD 脚手架项目**，按需使用 GSD 技能（`/gsd-code-review`、`/gsd-forensics`、`/gsd-map-codebase`）。未启用里程碑/阶段生命周期。

### 测试/CI 基础设施

- `src/test/`：**不存在** — 完全没有自动化测试
- `.github/workflows/`：**不存在** — 没有 CI
- `.git/hooks/`：仅有示例文件（无 pre-commit、pre-push 或 commit-msg 钩子）

### 文档漂移（代码 ↔ 文档滞后）

| 文件 | 最后修改时间 | 最后修改提交 | 备注 |
|---|---|---|---|
| `AGENTS.md` | 2026-06-29 11:39 | da28607 | 仅路径修复 — 06-28 创建，06-29 修改 |
| `CHANGELOG.md` | 2026-06-29 11:39 | da28607 | 仅路径修复 |
| `docs/update-1.0.5.md` | 2026-06-29 11:39 | da28607 | 仅路径修复 |
| `src/main/resources/data/csgobox/recipe/*.json` | 2026-06-29 09:00 | 24a82ac | 此处实际重命名了配方文件 |
| `.planning/codebase/STACK.md` | 2026-06-29 09:14 | e534161 | 仅 token 重命名 |

**代码级配方修复与文档级配方修复之间的时间差：约 2 小时 39 分钟。**

---

## 检测到的异常

### 异常 1 — v1.0.5 发布未通过构建与冒烟测试（置信度：高）

**证据：**
- `git ls-tree -r 862ab1f | grep CsboxConfig` 无返回结果。发布标签中没有 `CsboxConfig.java`，尽管约 10 个消费点导入了 `com.reclizer.csgobox.config.CsboxConfig`。
- `b7b11e5`（发布后）的标题就是 `fix(config): relocate CsboxConfig to csgobox package` — 添加缺失的类以使发布能够编译。
- `v1.0.5-REVIEW.md` CR-001：`init()` 是死代码 → 所有配置驱动值在运行时读取为 0/null。
- `v1.0.5-REVIEW.md` CR-002：`switch (CONFIG.animationSpeed)` 在首次动画 tick 时抛出 NPE — 会在玩家打开任何 CS:GO 箱子的瞬间崩溃。

**解读：** `862ab1f` 在没有针对该标签运行 `./gradlew build` 的情况下就打上了标签，也没有启动客户端打开箱子。两者都是该代码库的最低限度冒烟测试，任何一个都会在几秒内暴露损坏的导入。发布提交是由未经验证的部分状态拼凑而成的。

---

### 异常 2 — 发布后热补丁爆发（置信度：高）

**证据：** 发布后约 24 小时 35 分钟内有 8 个提交；其中 3 个是实质性修复（be40e5a breaking 配方变更、b7b11e5 配置类修复、a8bea6a JDK 锁定），其余是文档补录。

**解读：** 发布后跟随着一波紧急清理，作者在此过程中发现并修复了本应在发布前捕获的实际缺陷。这种模式（发布 → 热补丁集群 → 文档补录）是经典"未经验证就发布"的特征。`v1.0.5-REVIEW.md` 中记录的 **CRITICAL** 级 Bug（CR-001/CR-002/CR-003）在发布后的任何提交中都**没有**被修复——它们是在用户调用 `/gsd-code-review` 后才被识别出来的（现已提交为 `5f3856a`）。它们在当前 `main` HEAD 上仍然是未修复状态。

---

### 异常 3 — 代码修复后文档滞后（置信度：高）

**证据：** 配方 JSON 文件在提交 `24a82ac`（2026-06-29 09:00）中从 `data/csgobox/recipes/` 移动到 `data/csgobox/recipe/`。文档（AGENTS.md / CHANGELOG.md / docs/update-1.0.5.md）直到 `da28607`（2026-06-29 11:39）仍引用复数路径 — **2 小时 39 分钟的漂移**，在此期间文档与文件系统不一致。

**解读：** 代码端重命名没有与文档端重命名在同一提交中配对。这是一个流程缺口：一个"代码重命名时同步更新文档"的规则本可以在源头捕获此问题。没有这一规则，同样的漂移模式将在下次重命名时重复出现（配方文件 → mod-id 路径等）。

---

### 异常 4 — 无测试、无 CI、无 Pre-Commit 钩子（置信度：高）

**证据：**
- `src/test/` 不存在。
- `.github/workflows/` 不存在。
- `.git/hooks/` 仅包含默认示例文件；未安装项目特定钩子。

**解读：** 所有四个 v1.0.5 关键 Bug 都是可测试的，其中几个仅靠钩子就能捕获：

| Bug | 可被以下手段检测到 |
|---|---|
| CR-001（init() 死代码） | 单元测试：构造 CsboxConfig + 断言配置值与 schema 默认值一致 |
| CR-002（switch 上 NPE） | 单元测试：将 null 枚举传入 switch — 立即失败 |
| CR-003（掉落率 = 0） | 单元测试：断言加载后 `CONFIG.globalDropRatePercent == 100` |
| CR-004（配方路径） | Pre-commit 钩子：grep `data/<ns>/recipes/` 并拒绝 |

以上检查均不存在。验证层完全是手动的。

---

### 异常 5 — 未正式创建发布标签（置信度：中）

**证据：** `git tag --list 'v*'` 中只有 `v1.0.0`。发布提交 `a86c8c4`（v1.0.4）和 `862ab1f`（v1.0.5）— 两者的标题都是 `release: vX.Y.Z` — 从未被打标签。

**解读：** 发布仅通过提交消息文本和 CHANGELOG 条目传达。消费者无法 `git checkout v1.0.5` 来复现构建。这使得发布变成单个提交点，没有不可变引用。对于在 CurseForge/Modrinth 分发的模组来说，缺少 v1.0.5 标签意味着没有规范的制品可供发布。

---

### 异常 6 — 构建制品存在于仓库历史中（置信度：中）

**证据：** `git log --name-only` 显示提交 `bc96f4a` 和 `c0e060e` 中反复包含 `build_log.txt`、`build_output.txt`、`gradle_stderr.txt`、`gradle_stdout.txt`、`output.txt`、`stderr.txt`、`stdout.txt`、`tee_out.txt`、`final_out.txt`。`.gitignore` 现在已覆盖这些文件（`/*.txt`、`build/`），因此该问题是历史性的，而非当前问题。

**解读：** `.gitignore` 收紧之前的提交污染了历史。不是回归问题，但是一次性的清理机会（如果项目希望拥有更精简的历史，可以使用 `git rm --cached` + filter-repo）。

---

### 异常 7 — 卡循环/热点文件（置信度：中）

**证据：** 在过去 10 个提交中：
- `AGENTS.md`：4 次触碰（617cbe7 创建、be40e5a 修改、da28607 修改）
- `CHANGELOG.md`：4 次触碰（a86c8c4、862ab1f、be40e5a、da28607）
- `docs/update-1.0.5.md`：3 次触碰（862ab1f、be40e5a、da28607）
- `.planning/codebase/*.md`：多次触碰 — 24a82ac 和 e534161 都对相同的 6 个文件进行了重命名更新。

**解读：** 不是真正的"死循环"（提交消息各不相同），而是一种**重复触碰模式**，表明文档文件没有与代码实时保持同步。与异常 3 同源。

---

## 根因假设

v1.0.5 发布时已损坏，因为**在"代码看起来没问题"和"打标签发布"之间不存在验证关卡**。具体来说：

1. **无自动化构建。** `./gradlew build` 本会因缺少 `CsboxConfig` 类而编译失败。
2. **无冒烟测试。** 没有在客户端侧打开 CS:GO 箱子的测试，这本来会暴露 CR-002（NPE）和 CR-001/CR-003（掉落率 = 0）。
3. **无配方加载测试。** 没有数据包加载冒烟测试，这本来会暴露 CR-004（recipes/ vs recipe/）。
4. **无文档/代码一致性钩子。** `24a82ac` 中的配方路径重命名静默地让 3 个文档文件在 2 小时 39 分钟内引用了旧路径。
5. **无正式发布标签。** 发布仅作为提交存在，没有用于分发的不可变 v1.0.5 引用。

发布后的热补丁集群（24 小时内 8 个提交）是症状——作者在发布后手动查找并修复了他们能修复的所有问题。代码审查（`v1.0.5-REVIEW.md`，提交为 `5f3856a`）暴露了手动检查遗漏的三个更多 Bug（CR-001/CR-002/CR-003），而这些在 `main` HEAD 上**仍未修复**。

---

## 建议措施

### A. 修复三个剩余的关键 Bug（按顺序）

1. **CR-001 + CR-002 + CR-003 — CsboxConfig init() Bug**
   文件：`src/main/java/com/reclizer/csgobox/config/CsboxConfig.java`
   - 要么按照 AGENTS.md 规范在构造方法中内联 `.get()`（推荐——v1.0.5-REVIEW.md 中的建议修复方案），要么将 `init()` 连接到 `ModConfigEvent.Loading`/`Reloading`。
   - 单个原子提交。改动微小但行为影响重大。

2. **端到端重新测试 recipes/→recipe/ 重命名**
   - 确认 `build/libs/csgobox-1.0.5.jar` 确实包含 `data/csgobox/recipe/csgo_key3_smithing.json`（而非 `recipes/`）。
   - 在测试世界中用每级钥匙手动打开 CS:GO 箱子。

### B. 添加缺失的验证层

1. **Pre-commit 钩子：** 添加项目本地 `.githooks/pre-commit`，用于：
   - 拒绝任何修改 `src/main/resources/data/` 但没有同步更新提及相同路径的文档的提交，或者
   - 运行 `./gradlew compileJava` 并在失败时拒绝（v1.0.5 的导入 Bug 本会被此钩子捕获）。
   - 通过 `git config core.hooksPath .githooks` 启用。

2. **冒烟测试脚本：** `scripts/smoke-test.sh`，运行 `./gradlew build` + 解压生成的 jar + 断言 jar 中存在预期的配方路径 + 在 jar 中 grep `RecipeSerializer` 注册的 `csgo_key3_smithing`。接入 `Makefile` 或 `pre-push` 钩子。

3. **CsboxConfig 的单元测试（最低限度）：** 三个测试本可捕获 CR-001/CR-002/CR-003：
   ```java
   @Test void configValuesMatchSchemaDefaults();   // 断言 .get() 返回 schema 默认值
   @Test void animationSpeedNeverNull();           // 断言枚举字段已填充
   @Test void dropRatePercentHundredByDefault();   // 断言 > 0
   ```
   JUnit 5 已是 NeoForge 的传递依赖；只需添加 `src/test/java/` 和 testImplementation 依赖。

4. **CI（可选但成本低）：** 一个 `.github/workflows/build.yml`，在每个 PR 上运行 `./gradlew build` + 冒烟测试脚本。在损坏的发布到达 `main` 之前将其捕获。

### C. 正确打发布标签

在 A 和 B 之后：
- 将清理后的 HEAD 打标签为 `v1.0.5`（不 bump 补丁版本号——由项目所有者决定是否保持 1.0.5 版本标签，尽管修复是在原始 1.0.5 提交 862ab1f 之后才落地）。v1.0.5 标签取代了从未正式打标签的隐式 v1.0.5 引用。
- 推送标签：`git push origin v1.0.5`。

### D. 更新 AGENTS.md 以正式化新规则

在 AGENTS.md 中添加：
- "Pre-commit 钩子：需要构建 + 配方路径一致性检查。"
- "发布标签：每个 `release: vX.Y.Z` 提交必须在同一推送中用 `git tag vX.Y.Z` 跟進。"
- "文档/代码一致性：`src/main/resources/` 中的任何路径重命名必须与 AGENTS.md / CHANGELOG.md / docs/update-X.Y.Z.md 中的相应更新在同一提交中配对。"

---

## 本报告未覆盖的内容

- **未创建 GitHub issue。** 此报告仅为调查报告。
- **未修改任何代码。** 取证技能在设计上为只读；CR-001/CR-002/CR-003 的 Bug 修复不在本次运行范围内。
- **未创建 STATE.md / ROADMAP.md。** 项目缺乏 GSD 脚手架；引入它是项目所有者应单独做出的决定。

---

*报告由 `/gsd-forensics` 生成。异常基于：提交时间戳、`git ls-tree -r 862ab1f` 输出、`.planning/` 目录列表、`.git/hooks/` 列表、`src/test/` 和 `.github/workflows/` 不存在。所有路径已脱敏以方便移植（去掉了 `$HOME/Desktop/CS2-Box/` 前缀）。*
